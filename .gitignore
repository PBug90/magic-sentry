# Rust
/target

# CLI game output files (generated at runtime, not project assets)
/cli/*.json
/cli/*.html

# Node
node_modules/

# Compiled build output for every package
dist/

# Web server
/web/.env
/web/.env.test

# wc3data — SLK game data files (place in wc3data/slk/ to regenerate pipeline)
/wc3data/slk/*.slk

# wc3data — intermediate JSON generated from SLK files
/wc3data/src/UnitBalance.json
/wc3data/src/UpgradeBalance.json
/wc3data/src/ItemBalance.json

/web/public/favicon.ico

# Twitch extension
/twitch-extension/.env
/twitch-extension/*.zip

# Claude
.claude

# Superpowers brainstorm sessions
.superpowers/

# Standalone report — generated icon map (build artifact)
/web/src/client/standalone/iconMap.ts

# Liquipedia scraper cache
shared/.liquipedia-cache/
