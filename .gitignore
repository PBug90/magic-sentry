# Rust
/target

# CLI game output files (generated at runtime, not project assets)
/cli/*.json

# Node
node_modules/

# Web server
/web/.env
/web/.env.test
/web/dist/

# Shared package compiled output
/shared/dist/

# Shared — SLK game data files (place in shared/slk/ to regenerate pipeline)
/shared/slk/*.slk

# Shared — intermediate JSON generated from SLK files
/shared/src/UnitBalance.json
/shared/src/UpgradeBalance.json
/shared/src/ItemBalance.json
/web/public/favicon.ico

# Twitch extension
/twitch-extension/dist/
/twitch-extension/node_modules/
/twitch-extension/.env

# Claude
.claude

# Superpowers brainstorm sessions
.superpowers/

# Standalone report — generated icon map (build artifact)
/web/src/client/standalone/iconMap.ts
