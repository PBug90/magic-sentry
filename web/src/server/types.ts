// Mirror of magic-sentry-cli/src/types.rs — single source of truth in @magic-sentry/shared.
export type {
  HeroSample,
  UnitSnapshot,
  Sample,
  HeroFinal,
  UnitSummary,
  PlayerSummary,
  PlayerRecord,
  GameRecord,
  PlayerPatch,
  GamePatch,
} from '@magic-sentry/shared'
